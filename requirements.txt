# ═══════════════════════════════════════════════════════════════════
#   🔐 SECURE VAULT PRO — Зависимости
#   Зашифрованный сейф с защитой от брутфорса
# ═══════════════════════════════════════════════════════════════════

# ── GUI Framework ─────────────────────────────────────────────────
PySide6>=6.6.0
# Qt6 for Python — профессиональный GUI toolkit
# Включает: QtCore, QtGui, QtWidgets, QtNetwork, QtSvg

# ── Cryptography ──────────────────────────────────────────────────
cryptography>=42.0.0
# AES-256-GCM шифрование файлов
# Fernet, HKDF, X.509 и другие криптографические примитивы

argon2-cffi>=23.1.0
# Argon2id — современный KDF (Key Derivation Function)
# Победитель Password Hashing Competition 2015
# Устойчив к GPU/ASIC-атакам

# ── Build Tools (опционально, для сборки .exe) ────────────────────
pyinstaller>=6.0
# Компиляция Python → standalone .exe
# Поддержка PySide6 через --collect-all

# ── Image Processing (опционально, для генерации иконки) ─────────
pillow>=10.0.0
# Генерация иконки 1.ico если её нет
# Обработка изображений

# ═══════════════════════════════════════════════════════════════════
#   УСТАНОВКА:
#   pip install -r requirements.txt
#
#   БЫСТРАЯ УСТАНОВКА (только runtime):
#   pip install PySide6 cryptography argon2-cffi
#
#   СБОРКА В .EXE:
#   python build.py  (автономный сборщик всё установит сам)
# ═══════════════════════════════════════════════════════════════════